# MiniGPT from Scratch — Dependencies
# ─────────────────────────────────────────────────────────────
# Install:
#   pip install -r requirements.txt
#
# GPU (CUDA 12):
#   pip install -r requirements.txt --extra-index-url \
#       https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
#
# Google Colab already has JAX; run:
#   !pip install flax optax orbax-checkpoint gradio tiktoken datasets tqdm
# ─────────────────────────────────────────────────────────────

# Core JAX ecosystem
jax[cpu]>=0.4.25            # swap for jax[cuda12] on GPU
flax>=0.9.0                 # Flax NNX
optax>=0.2.2                # Optimisers & gradient transforms
orbax-checkpoint>=0.6.0     # Checkpointing

# Tokenisation & datasets
tiktoken>=0.6.0             # OpenAI BPE tokeniser (optional, for larger models)
datasets>=2.18.0            # HuggingFace datasets (optional)

# Chat interface
gradio>=4.25.0

# Utilities
numpy>=1.26.0
matplotlib>=3.8.0
tqdm>=4.66.0